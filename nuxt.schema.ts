export default defineNuxtSchema({
  appConfig: {
    /**
     * pp-widgets configuration.
     *
     * @studioIcon material-symbols:docs
     */
    pp: {
      /**
       * Website title, used as header default title and meta title.
       *
       * @studioIcon material-symbols:title
       */
      title: 'pp-widgets',
    }
  }
})
